<?xml version="1.0" encoding="utf-8"?>
<powertime>
	<!-- 非树形表必须设置 maxLevel 值，否则导致死循环 
		属性key，表示将父节点pid的值与当前节点的第一个字段的值，结合成为新的 字段值。
		例如：key="id" 在二级节点中，生成字段 id=pid+"."+第一个字段的值（id AS 'key'）
	-->
	<trees id="2001.001" maxLevel="2" database="systemDatabase"
		sql="SELECT id,name AS paraName,varname AS varName,description AS mark,id AS 'key' FROM user_dict_type WHERE status=1">
		<tree key="id" sql="SELECT id As 'key',name AS paraName,value AS varName FROM user_dict WHERE (status=1 AND type_id=?)"/>
	</trees>
</powertime> 
